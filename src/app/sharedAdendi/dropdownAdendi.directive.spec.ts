/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DropdownAdendiDirective } from './dropdownAdendi.directive';

describe('Directive: DropdownAdendi', () => {
  it('should create an instance', () => {
    const directive = new DropdownAdendiDirective();
    expect(directive).toBeTruthy();
  });
});
